diff --git a/src/app/who-we-are/HistoryScrollButton.tsx b/src/app/who-we-are/HistoryScrollButton.tsx
deleted file mode 100644
index 7d5c6dc..0000000
--- a/src/app/who-we-are/HistoryScrollButton.tsx
+++ /dev/null
@@ -1,66 +0,0 @@
-"use client";
-
-import { useEffect, useState } from "react";
-
-function canScrollTimeline(shell: HTMLElement) {
-  return shell.scrollWidth - shell.clientWidth > 12;
-}
-
-export function HistoryScrollButton() {
-  const [isAtEnd, setIsAtEnd] = useState(false);
-
-  useEffect(() => {
-    const shell = document.querySelector<HTMLElement>(".who-timeline-shell");
-
-    if (!shell || !canScrollTimeline(shell)) {
-      return;
-    }
-
-    const updateState = () => {
-      const remaining = shell.scrollWidth - shell.clientWidth - shell.scrollLeft;
-      setIsAtEnd(remaining < 24);
-    };
-
-    updateState();
-    shell.addEventListener("scroll", updateState, { passive: true });
-    window.addEventListener("resize", updateState);
-
-    return () => {
-      shell.removeEventListener("scroll", updateState);
-      window.removeEventListener("resize", updateState);
-    };
-  }, []);
-
-  const handleClick = () => {
-    const shell = document.querySelector<HTMLElement>(".who-timeline-shell");
-
-    if (!shell) {
-      return;
-    }
-
-    const maxScroll = shell.scrollWidth - shell.clientWidth;
-    const nextLeft = isAtEnd
-      ? 0
-      : Math.min(shell.scrollLeft + Math.max(shell.clientWidth * 0.86, 760), maxScroll);
-
-    shell.scrollTo({
-      behavior: "smooth",
-      left: nextLeft,
-    });
-  };
-
-  return (
-    <button
-      aria-label={isAtEnd ? "Return to the start of the history timeline" : "Scroll right through the history timeline"}
-      className={`who-journey-button ${isAtEnd ? "is-at-end" : ""}`}
-      onClick={handleClick}
-      type="button"
-    >
-      <span>{isAtEnd ? "Back To Start" : "See The Whole Journey"}</span>
-      <svg aria-hidden="true" className="who-journey-icon" fill="none" viewBox="0 0 32 32">
-        <circle cx="16" cy="16" r="13" stroke="currentColor" strokeWidth="2" />
-        <path d="M14 10L20 16L14 22" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="2.4" />
-      </svg>
-    </button>
-  );
-}
